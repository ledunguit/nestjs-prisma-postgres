{
  "common": {
    "min": "$t(fields.{property}) length must be greater than {constraints.0}",
    "max": "$t(fields.{property}) length must be less than {constraints.0}",
    "string": "$t(fields.{property}) must be a string",
    "exists": "$t(fields.{property}) is already exists",
    "not-exists": "$t(fields.{property}) is not exists",
    "not-empty": "$t(fields.{property}) must not be empty",
    "email": "$t(fields.{property}) must be a valid email address, current value: {value}"
  }
}